<?xml version="1.0"?>
<root BTCPP_format="4">
    <BehaviorTree ID="ApproachDescribedTarget"> 
      <Sequence>                                                         
        <GroundDescriptionAction description="{description}" roi="{roi}" stamp="{stamp}" setpoint_offset="{setpoint_offset}"/>
        <ApproachTargetAction roi="{roi}" stamp="{stamp}" setpoint_offset="{setpoint_offset}"/>
      </Sequence>
    </BehaviorTree>
</root>